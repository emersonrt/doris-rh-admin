import { MaskDataMesAnoPipe } from './mask-data-mes-ano.pipe';

describe('MaskDataMesAnoPipe', () => {
    it('create an instance', () => {
        const pipe = new MaskDataMesAnoPipe();
        expect(pipe).toBeTruthy();
    });
});
